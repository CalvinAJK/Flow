@model IEnumerable<Flow.Data.Task>

@if (Model != null && Model.Any())
{
    <h2>Tasks:</h2>
    <ul>
        @foreach (var task in Model)
        {
            <li>@task.Name - @task.Description</li>
        }
    </ul>
}
else
{
    <p>There are no existing tasks in this project yet.</p>
}